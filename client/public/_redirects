# Handle static assets first (these should NOT redirect to index.html)
/static/*  /static/:splat  200
/favicon.ico  /favicon.ico  200
/manifest.json  /manifest.json  200
/logo*.png  /logo*.png  200
/robots.txt  /robots.txt  200

# Handle client-side routing for React Router (catch-all for everything else)
/*    /index.html   200